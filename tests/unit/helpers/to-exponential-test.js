import {
  toExponential
} from 'retrostalgia/helpers/to-exponential';

module('ToExponentialHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = toExponential(42);
  ok(result);
});
